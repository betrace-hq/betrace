{
  "name": "betrace-integration-tests",
  "version": "1.0.0",
  "description": "Cross-platform integration tests for BeTrace",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "PWDEBUG=1 playwright test"
  },
  "keywords": ["betrace", "integration-tests", "playwright"],
  "author": "BeTrace Team",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "axios": "^1.6.0"
  }
}
